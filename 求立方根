#include<stdio.h>
#include<math.h>
int main(){
	int n;
	scanf("%d",&n);
	while(n>0){
		double a,res;
		scanf("%lf",&a);
		if(a>=0)res=pow(a,1.0/3);
		else res=-pow((-a),1.0/3);
		printf("%.6lf\n",res);
	n--;
	}
    return 0;
}
